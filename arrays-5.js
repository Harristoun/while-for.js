const arr = ['Arjan', 'vishan', 'ahmad', 'Radjab', 'linma', 'arigami', 'volva', 'i eshe kakoe-to imya'];
for (let i = 0; i < arr.length; i++) {
    if (arr[i][0] === 'a' ) {
        console.log(arr[i])
    }
    
}
