const arr = ['Swift','SQL','Assembly language','JavaScript','Visual Basic','C#','C++','Java','C','Python'];
let accomulator = [];

for (let i = 0; i < arr.length; i++) {
    if (arr[i].length > 3) {
        accomulator.push(arr[i])
        
        
    }
    
}